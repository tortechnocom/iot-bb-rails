pipeline {
  agent any
  stages {
    stage('dev') {
      steps {
        sh 'bundle install'
      }
    }
    stage('staging') {
      steps {
        sh 'bundle install'
      }
    }
    stage('production') {
      steps {
        sh 'bundle install'
      }
    }
  }
}